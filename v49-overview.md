# 小彘的工作台 v49 更新说明

> 已部署到 GitHub Pages（master 分支，commit `f997112`，BUILD `2026-08-14-v49`）。
> 单文件 PWA，`index.html` + `sw.js`，数据本地 localStorage，媒体 IndexedDB。

## 10 项反馈全部完成

| # | 反馈 | 本次处理 |
|---|------|----------|
| 1 | 视觉灵感「全部图片」没封面 | 用最新保存的那张图作封面（`list[0].media`），无图才显示占位符 |
| 2 | 老是弹「保存密码」 | 3 个 `password` 输入改为 `text` + CSS `text-security:disc` 掩码，Chrome 不再当密码字段 → 不弹窗 |
| 3 | 英语积累深度优化 | ①对话加中文翻译；②朗读改选美式真人嗓(en-US 自然语音)；③每日一句加🔊朗读按钮；④测试按 3/7/14 天间隔复习、连对 4 次进「已学库」、答错明天再练（新建 SRS 结构 `english_srs`）；⑤翻译对照后加「🤖 让 AI 分析我的翻译」按钮；⑥首页英语计数改 **/5**（4 学习 + 1 测试） |
| 4 | 生活万花筒首页缩小 | 缩成一行 4 个数字（必做/英语/金币/菜），点一下直接进生活万花筒 |
| 5 | 转盘问题 | ①指针改为固定层，转盘转指针不转；②结果在转盘**中心从小放大(bingo)**显示，点一下进做菜栏；③做菜栏菜品文字压缩紧凑 |
| 6 | 灵感速记编辑/复制时灵时不灵 | 未整理列表与灵感池每条左下角加 ✎ 编辑 / ⧉ 复制 两个常显按钮（替代长按复制） |
| 7 | 长按变灰不恢复 | DragSort 加全局兜底：任何触摸/指针结束都清除残留 `.dragging`(变灰) |
| 8 | 删了习惯又冒出来 | `ensureSeeds` 用 `_trackerSeeded` 标志只种一次，之后绝不复活已删默认习惯 |
| 9 | 份子钱编辑 + 再记一笔 | 每条加 ✎ 编辑、↻ 再记一笔（复制账单改细节）、✕ 删除；事件输入框变下拉（按使用频次从高到低） |
| 10 | 金币获取扩充 | 写经验摘录也 +1 金币（每天最多 1，已计入金币来源列表） |

## 获得金币的方式（当前全部）
完成每日记录、完成复盘、完成心愿、运动打卡、学会新菜、英语全勤、完成英语测试、写情绪日记、连续 3 天、连续 7 天、**写经验摘录**。

## 验证
- `node --check` 主脚本语法通过。
- 无残留对已删除函数（`getQuiz`/`reviewQueue`/`addToReview`/`Gift.add`）的引用。
- 旧转盘指针 CSS（`.wheel:before` / `.wheel-mini-disk::after`）已移除。

## 上线提醒
GitHub Pages 构建约需 1 分钟。若在手机/桌面看到旧版，强制刷新（清缓存）即可。
