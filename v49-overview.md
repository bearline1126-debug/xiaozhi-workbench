# 小彘的工作台 — v49 / v50 更新说明

**线上地址**：https://bearline1126-debug.github.io/xiaozhi-workbench/
**版本**：`2026-08-14-v50`（已部署并验证生效）

你提的 10 项反馈全部改完了。下面按你的编号对应说明。

---

## 1. 视觉灵感「全部图片」补封面 ✅

之前那个卡片只有一个占位方块 `▦`。现在改成自动取**你最新保存的那张图**当封面，有新图就跟着换。卡片本身照旧显示，位置不变。

## 2. 不再弹「保存密码」了 ✅

根因是 3 个输入框用了 `type="password"`，Chrome 一看到就自动弹密码管理器。现在改成普通文本框 + CSS 遮点（`-webkit-text-security`），**看起来还是圆点、一样看不见内容**，但 Chrome 不再认为这是密码框，弹窗彻底消失。

涉及：解锁密码、AI 密钥、修改新密码。

## 3. 英语积累深度优化 ✅

- **对话加中文翻译**：每段对话下面直接跟中文。
- **语音换成美式真人嗓**：原来没指定嗓音，系统随便挑一个（所以难听）。现在会在你手机/电脑的语音库里**优先挑 en-US 的真人嗓**，挑不到才退回默认。
- **每日一句加了播放按钮**：第一个卡片现在也能点着听。
- **测试卡改成间隔复习（重点）**：
  - 答对一次 → 隔 **3 天**再测；再答对 → 隔 **7 天**；再答对 → 隔 **14 天**
  - **连续答对 4 次**才进「已学习库」，从此不再测你
  - **中途错一次，连对次数清零**，当天就重新排进待测库，继续测到 4 次为止
  - 卡片上实时显示「已连对 2/4 · 还差 2 次进已学库」
- **翻译后加了 AI 分析按钮**：点一下，把**题目 + 你的翻译 + 参考答案**一起发给 AI，让它指出你该注意什么、怎么翻更准。
- **首页计数改成 /5**：确实是 4 项学习 + 1 项测试 = 5，之前写死成 /4 了。

## 4. 生活万花筒首页缩小 ✅

原来是 2×2 的四宫格，占地方。现在改成**一行横排四项统计**，四个数据一眼看全，整张卡片点一下就进生活万花筒。

## 5. 转盘与做菜栏 ✅

- **指针不再跟着转**：根因是指针原本画在转盘元素内部（`.wheel:before`），转盘一转它当然跟着转。现在把指针**移出来做成独立固定层**，只有盘转、针不动。
- **结果改在转盘中心弹出**：转完之后，结果直接**从转盘正中央由小放大弹出来**（你说的那种 bingo 效果）。首页小转盘和做菜页大转盘都一样。
- **点结果直接进做菜栏**：弹出来的结果可以点。
- **首页转了看不到结果的问题一并解决**：以前结果只写在下面的小字里，首页那个位置太挤看不见，现在中心弹出，跑不掉。
- **菜品文字压缩**：菜名、标签、步骤分成三级字号，紧凑了很多，不再一道菜占半屏。

## 6. 灵感速记加编辑/复制按钮 ✅

未整理列表每条**左下角加了两个符号**：

- `✎` 点了直接进编辑状态
- `⧉` 点了直接复制这条内容

复制做了双通道兜底（新剪贴板 API + 老方法），不会再像长按那样时灵时不灵。原来点文字编辑的方式也保留着。

## 7. 长按变灰不恢复 ✅

根因是某些情况下手指抬起的事件没被捕获，变灰的样式类就一直挂着。现在加了一道**全局兜底清理**：不管是正常松手、意外中断还是被系统打断，只要触摸一结束就把页面上所有残留的变灰状态一次扫干净。

## 8. 追踪表习惯删不干净 ✅（这是个真 bug）

根因找到了：启动时有个「补种默认习惯」的逻辑，它会检查默认习惯里**哪些不在你的列表里，就补回来**——所以你删掉的正好每次都被当成"缺失"补回。

现在改成：**只有第一次使用（列表完全不存在）才种默认习惯**。已经在用的用户，删了就是删了，永不补回。

## 9. 份子钱编辑与再记一笔 ✅

- `✎` **编辑**：点了把这笔载入表单，改完点「更新记录」（按钮文字会自动变）。
- `↻` **再记一笔**：复制这笔账单的人名、金额、事件、备注，**日期自动改成今天**，你改改细节直接存，不用重新填一遍。
- **事件变成下拉可选**：输入过的事件会自动进下拉列表，**按你用的次数从多到少排**，常用的排最前面。以后同一个事件输一次就够了。

## 10. 金币获取方式扩充 ✅

- **写经验摘录 +1 金币**，一天最多 1 枚（系统按"来源+日期"去重，天然做到一天一次）。
- **兑换铺新增「🪙 金币怎么赚」清单**：原来那行小字提示内容早就过期了。现在改成动态清单，**把全部获取方式列出来**，每条显示图标、说明、以及「今天已领 ✅ / 待领取」，顶上还写着今天已赚多少、每天上限多少。

### 目前一共 11 种赚金币的方式（每种每天 1 次，全天上限 10 枚）

| 方式 | 怎么触发 |
|---|---|
| 📝 完成每日记录 | 填了感恩日记、习惯或情绪 |
| 🧠 完成复盘 | 做一次周/月复盘 |
| ✅ 完成心愿 | 标记一个心愿/行动为已完成 |
| 💪 运动打卡 | 记录一次运动 |
| 🍳 学会新菜 | 新增一道菜谱到做菜栏 |
| 📖 英语全勤 | 一天内完成全部 4 项学习 |
| 🧪 完成英语测试 | 完成今天的复习测试 |
| 📒 **写经验摘录（新增）** | 在经验摘录里写一篇笔记 |
| 💭 写情绪日记 | 记录一条情绪日记 |
| 🔥 连续 3 天 | 连续打开工作台 3 天 |
| ⭐ 连续 7 天 | 连续打开工作台 7 天 |

---

## 使用提示

手机上如果还看到旧界面，**把页面彻底关掉重开一次**（或下拉刷新两次）。设置里的版本号显示 `2026-08-14-v50` 就是最新的了。

你原有的数据（记录、图片、习惯、份子钱等）全部保留，这次改动没有动任何数据结构，只有第 8 项修掉了"自动补回默认习惯"的行为。
