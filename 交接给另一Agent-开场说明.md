# 小彘的工作台 — 交接给另一 Agent 的开场说明（可直接复制）

> 用法：把下面「一、开场白」+「二、项目关键信息」整段复制给另一个 Agent 作为第一句话；
> 「三、变更摘要」按当轮实际改动替换；「四、五」是他要遵守的约定与红线。

---

## 一、开场白（粘贴给另一 Agent 的第一句）

我有个单文件 PWA 个人仪表盘「小彘的工作台」，现在交给你继续迭代。请先读项目根目录的 `AGENTS.md`（这是唯一规则入口，开工前必须读，里面写了目录、边界、版本号机制、部署方式、交付约定）。读完按我的需求批次动手。

## 二、项目关键信息（贴给他看）

- 工作目录（只能在这里改）：`C:\Users\cheng\Documents\Codex\2026-08-10\skill-1-skill-skill-md-2\小彘的工作台 - v2-workbuddy`
- 当前版本：v92（`2026-08-22-v92`）
- 线上地址：https://bearline1126-debug.github.io/xiaozhi-workbench/
- 代码结构：全部逻辑在 `index.html` 一个文件内联（HTML+CSS+JS，约 4MB）；`sw.js` 是 Service Worker；数据全在 localStorage，不上云。
- 部署：改完必须跑 `bash deploy_github.sh`（推送 GitHub Pages），随后 `curl` 验证线上 `BUILD_VERSION` 已变。
- 进入文件夹后必须同时看到 `.git` / `deploy_github.sh` / `AGENTS.md` 三件套，缺任一就是找错目录，立刻停下。

## 三、近期变更摘要（已上线，供他接手时了解现状）

- v92：① 闲话铺 AI 去刻板化（system prompt 从 10 条带反例「铁律」改为只给方向）；② 今日必做改为可编辑 / 可补（原只读展示）；③ 欢迎语改为每天只变一次（去掉缓存判断的 `!localOnly`）。

> 每轮交付后把本段更新为当轮摘要即可。

## 四、我怎么给你提需求（请你也照这个节奏回）

- 我一批发 6–8 条编号需求，常带截图；你看到 `@image` 截图要结合 UI 实际判断，别凭空想象。
- 你做完按 ① ② ③ 逐项回复，并附「变更摘要 + 线上地址」。
- 小步可验证，每次部署后主动 `curl` 验版本号。

## 五、他必须守的红线（摘自 AGENTS.md 第 3 / 4 节）

- 版本号三处同步（index.html + sw.js 的 CACHE / BUILD），漏改 = 手机不刷新 = 白部署。
- 旧副本 / 历史产物 / `English.content` 内联 JSON / `sw.js` 缓存策略，都别碰。
- 只在 canonical 目录改，其他同名文件夹一律是旧副本，不动。
- 改完 JS 必做 `node --check` 语法校验；遇到环境问题无法验证必须说明，不得假装通过。
