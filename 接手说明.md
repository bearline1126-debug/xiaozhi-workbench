# 小彘的工作台 — AI 接手说明（v27）

> 本文档供下一个接手的 AI 阅读。读完即可继续开发，无需向用户追问背景。

## 0. 项目一句话定位
「小彘的工作台」是一个**单文件 PWA 个人生产力仪表盘**，所有 HTML/CSS/JS 都在 `index.html` 一个文件里。手机浏览器打开后可「添加到主屏幕」当 App 用。当前由用户（中文沟通、偏好结构化验收）持续迭代，按批次提需求。

## 1. 文件位置 & 在线地址
- **项目根目录**：`C:\Users\cheng\Documents\Codex\2026-08-10\skill-1-skill-skill-md-2\小彘的工作台 - v2-workbuddy\`
- 核心文件：`index.html`（约 4342 行，唯一需要编辑的代码文件）、`sw.js`（Service Worker）、`manifest.json`
- 配套资源：`icon.png`、`icon-192.png`、`assets/`、`vercel.json`（暂用 GitHub Pages，vercel.json 是早期残留可忽略）
- **线上地址**：`https://bearline1126-debug.github.io/xiaozhi-workbench/`
- **GitHub 仓库**：`https://github.com/bearline1126-debug/xiaozhi-workbench.git`（**必须是 PUBLIC**，免费版 GitHub Pages 不支持私有库）
- 当前线上版本：`BUILD_VERSION = '2026-08-12-v27'`（index.html 与 sw.js 一致）

## 2. 部署流程（最关键，照做不会出错）
每次改完 `index.html` 后必须做四件事，**顺序不能乱**：

1. **改版本号**（否则用户手机不会刷新）：
   - `index.html` 顶部：`const BUILD_VERSION = '2026-08-12-vXX';`（XX 递增）
   - `sw.js` 顶部：`const CACHE = 'xiaozhi-workbench-vXX';` 和 `const BUILD = '2026-08-12-vXX';`（两处都要改，且要和 index.html 的版本号一致）
2. **语法校验**（必须先过，否则整页白屏）：
   ```
   node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');const m=html.match(/<script>([\s\S]*?)<\/script>/);try{new Function(m[1]);console.log('JS OK')}catch(e){console.error(e.message);process.exit(1)}"
   ```
3. **提交并推送**（⚠️ 推送命令极易写反导致卡死 2 小时，直接复制下面这行，把 TOKEN 换成当前有效 token）：
   ```
   git -c url."https://TOKEN@github.com/".insteadOf="https://github.com/" push origin master
   ```
   - 正确方向：`url."https://TOKEN@github.com/".insteadOf = "https://github.com/"`（把 github.com 重写成带 token 的地址）。**绝不能反写**。
   - 已沉淀为防错 Skill：`~/.workbuddy/skills/git-push-github/SKILL.md`，可用该 skill 代替手敲命令。
4. **验证上线**（GitHub Pages 构建约 1~2 分钟延迟）：
   ```
   curl -s "https://bearline1126-debug.github.io/xiaozhi-workbench/index.html" | grep -o "BUILD_VERSION = '[^']*'"
   ```
   确认返回新版本号即成功。若用户手机还显示旧版，让他清该站点缓存重开。

> 注意：`WorkBuddy` 的 `git push` 需在带 `dangerouslyDisableSandbox:true` 的 Bash 里跑（沙箱会拦截网络）。

## 3. 版本演进（v18→v25 都做了什么）
| 版本 | 核心改动 |
|---|---|
| v18 | 修复 PWA 安装角标（manifest `purpose` 改回 `any`、加 192/512 双图标） |
| v19 | 修复自定义图标 data URL 导致 Chrome 静默安装失败（sw.js 新增 `/custom-icon.png` 拦截，data URL→Blob） |
| v20 | 6 大功能：主线任务打卡 / 日记本翻书 / 睡眠+身体档案 / 作息曲线 / 感恩日记图片 / 碎碎念按天聚合 |
| v21 | UI 大精简；双页日记本；星愿瓶（emoji）；心愿行动归属（`wishId`） |
| v22 | 运动时长恢复；"大入口→更多"改名补全；日记本双页都显内容、月历下移、边缘翻页符号；星愿瓶完全重做（SVG 瓶+每年一个瓶+星星飞入） |
| v23 | 运动时长为下拉选项；日记本边缘翻页+空白页点击跳每日记录补记；星愿瓶星星动画改「落入」+ 记录移到底部 |
| v24 | 翻页动画重做（方向感知 flip-in/out，不再歪斜跳页）；本周汇总周日→周六排序+点日期退出周模式；运动时长加「自定」输入（单位 h 在 select 外）；金币重复领取给 toast 提示；星愿瓶记录区默认折叠 |
| v25 | 安全稳定体检：备份导入只接受「小彘的工作台」格式；导入时清洗自定义 App 名称/图标与 AI/同步地址；AI/同步地址只接受 https；云端拉取前自动留急救快照；Service Worker 只缓存本站文件，不再兜底外部接口请求 |
| v26 | 稳定与整理：每日记录/追踪表输入即自动保存到本机；App 自定义名称即时同步顶部、锁屏、标题与安装文案；分类页日记/知识/生活功能可隐藏并联动左侧栏；追踪统计删掉重复图表，改为可横向滑动的热力明细；manifest 增加 id、display_override、maskable 图标声明；启动时请求持久化存储 |
| v27 | 数据层「混合 Store」：localStorage 同步标记 + IndexedDB `bysdash-store/kv` 持久化，启动 `await Store.init()` 后即可同步读写，备份/恢复统一从 Store 走；自定义 App 图标功能下线，改用固定水彩图标（512 + 192 PNG）；**关键修复**：`sw.js` fetch handler 多一个 `)` 导致 SW 从未注册成功（这是 PWA「安装应用」仍带浏览器角标的根因之一），重写两个 respondWith 后 `node --check` 通过 |

## 4. 模块地图（`index.html` 内全是 `const Xxx = {}` 风格）
| 模块 | 行号 | 职责 |
|---|---|---|
| `Util` | 1114 | 日期运算、uid、esc、localStorage 便捷读写等基础工具 |
| `Store`/`Media` | 1075 起 / 1225 | 数据层：Store=混合（localStorage 同步标记 + IndexedDB `bysdash-store/kv` 持久化）；Media=IndexedDB 大文件（图片/视频） |
| `UI` | 1467 | toast、item 渲染、确认弹窗 |
| `App` | 1593 | 路由 `App.routes`、页面渲染调度 `App.go(id)`、`AppCustom` 自定义名称/图标 |
| `Home` | 1847 | 首页桌面（卡片网格 + 底部「更多」入口） |
| `Hub` | 1781 | 分类聚合（`groups`: diary/knowledge/life）、子页预览 |
| `Success`/`MustEditor` | 2111 / 2147 | 感恩日记 + 明日必做结构化编辑器 |
| `MainQuest` | 2198 | 主线任务（进度条、打卡 +1、关联必做） |
| `DiaryBook` | 2251 | **日记本**：双页翻书、`renderSpread`/`renderWeek`、月历 `calHtml`、空白页 `goDaily` 补记 |
| `Tracker` | 2398 | 习惯追踪表、`dayKey(date)` 按日期存取 |
| `Mood` | 2495 | 情绪日记 |
| `DailyLog` | 2506 | 每日记录页，`_pendingDate` 支持补记任意历史日期 |
| `Companion` | 2592 | 碎碎念/聊天，`dailyWhispers(date)` 按天聚合 |
| `Chart`/`AutoBackup` | 2703 / 2753 | 图表工具 / 自动备份 |
| `Review` | 2873 | 复盘中心（周/月计划、关联每日必做） |
| `Self`/`Notes`/`Quick` | 2911 / 2933 / 2992 | 个人档案 / 笔记 / 快捷 |
| `Wish` | 3000 | 心愿/行动（`wishId` 归属） |
| `WishShop` | 3077 | **兑换铺**：金币兑换 + 星愿瓶（`wishBottleArea`）+ 底部心愿记录 `wishDoneCardsArea` |
| `English` | 自 / 3231 | 英语积累 |
| `Coin` | 3346 | **金币系统**：`sources` 来源表、`earn`/`spend`/`total`、`DAILY_CAP=10`、每日上限与防重复 |
| `Visual`/`Sport`/`Outfit`/`Cooking`/`LifeAnalysis` | 3403 / 3454 / 3553 / 3635 / 3668 | 视觉灵感 / 运动打卡 / 穿搭 / 做菜 / 智能分析（统计运动时长 `sportMinutes` 等） |

## 5. 数据层 & 关键约定
- **按日期存取是常态**：感恩 `success:YYYY-MM-DD`、追踪 `tracker:YYYY-MM-DD`、必做完成 `task_done:YYYY-MM-DD`、运动 `sport_logs`(含 date/duration)、金币 `coin_history`、体重 `weight_logs`、身体档案 `body_profile`、用户消息 `companion_msgs`。
- 用户数据**全部存浏览器本地**（localStorage/IndexedDB），仓库公开也不泄露。
- 所有模块方法通过 `onclick="Module.method()"` 内联绑定，HTML 里大量用模板字符串拼装。
- 删代码后务必检查有没有**多余的 `}` 或 `},,` 双逗号**（曾两次因此整页 JS 报错，用第 2 节 node 命令可立刻定位）。
- 中文 UI；涨跌用红涨绿跌不适用（此处无股票），但文案默认中文。

## 6. 已知遗留 / 隐患（接手时心里有数）
1. **日记本翻页**：当前是「左右两页 + 方向感知滑动」近似翻书，用户仍觉得不够像真书（希望右页翻过去变左页的物理感）。CSS 在 `.book-left/.book-right` + `flip-in/flip-out` keyframes，可继续打磨。
2. **星愿瓶**：星星「落入」动画是 2D translateY 近似，不是真实 3D 入瓶；记录区已折叠在底部，但瓶本身视觉较粗糙。
3. **金币系统**：`earn` 同一天同一 source 只发一次（防重复），每日上限 10 枚——若用户反馈「加了某操作却没加币」，先确认是不是重复/触顶，而不是 bug。
4. **自定义图标功能已下线（v27）**：用户提供的固定水彩图作为图标，App 名称仍可自定义（sw.js 启动时动态注入到 manifest）。
5. **多 token 历史**：用户换过几次 GitHub token，旧的 `ghp_DH8F9...`/`ghp_ZJtG...` 建议去 GitHub 撤销；当前有效 token 由用户现场提供。

## 7. 给接手 AI 的注意事项
- 用户习惯：**一批次给多个编号需求**，做完后他按编号逐项验收。回复请按「①…②…③…」结构化，并附**变更摘要 + 线上地址**。
- 改动尽量小步、可验证；每次部署后主动 curl 验证版本号。
- 他常用截图（粘贴图）说明 UI 期望，看到 `@image` 务必结合图判断。
- 不要擅自重构整体结构，单文件 PWA 的模块边界已经稳定。
